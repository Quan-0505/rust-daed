diff --git a/crates/dae-outbound-core/src/lib.rs b/crates/dae-outbound-core/src/lib.rs
index b79fbc9..bd83ac7 100644
--- a/crates/dae-outbound-core/src/lib.rs
+++ b/crates/dae-outbound-core/src/lib.rs
@@ -1,3 +1,4 @@
+pub mod sticky;
 pub mod alive;
 pub mod annotation;
 pub mod anytls;
diff --git a/crates/dae-outbound-core/src/sticky.rs b/crates/dae-outbound-core/src/sticky.rs
new file mode 100644
index 0000000..57c6fed
--- /dev/null
+++ b/crates/dae-outbound-core/src/sticky.rs
@@ -0,0 +1,132 @@
+//! Sticky IP caching for proxy server connections.
+//!
+//! Ported from `olicesx/outbound` `dialer/stickyip` (the kdae engine's
+//! sticky-ip feature): within the TTL window the same resolved IP is reused
+//! for a proxy *domain*, so DNS rotation cannot cause per-connection address
+//! drift on the node dial. IP endpoints pass through untouched.
+
+use std::collections::HashMap;
+use std::net::{SocketAddr, TcpStream, ToSocketAddrs};
+use std::sync::{Mutex, OnceLock};
+use std::time::{Duration, Instant};
+
+/// How long a successful proxy IP resolution is reused.
+/// Mirrors kdae's CacheTTL (5 minutes, >= one health-check cycle).
+const STICKY_TTL: Duration = Duration::from_secs(300);
+
+struct StickyEntry {
+    /// Resolved "ip:port" connect address.
+    addr: String,
+    expires_at: Instant,
+}
+
+static STICKY_CACHE: OnceLock<Mutex<HashMap<String, StickyEntry>>> = OnceLock::new();
+
+fn cache() -> &'static Mutex<HashMap<String, StickyEntry>> {
+    STICKY_CACHE.get_or_init(|| Mutex::new(HashMap::new()))
+}
+
+/// Returns the sticky-resolved connect address for `endpoint`.
+///
+/// - Bare IP endpoints (with or without port) pass through unchanged.
+/// - Domain endpoints reuse a cached resolution within the TTL; otherwise they
+///   resolve once (first address) and cache the result.
+pub fn sticky_resolve(endpoint: &str) -> String {
+    // Fast path: already a concrete socket address (e.g. "1.2.3.4:443").
+    if endpoint.parse::<SocketAddr>().is_ok() {
+        return endpoint.to_string();
+    }
+    let (host, port) = split_host_port(endpoint);
+    // Bare IP host (e.g. "1.2.3.4" without port).
+    if host.parse::<std::net::IpAddr>().is_ok() {
+        return endpoint.to_string();
+    }
+
+    let key = endpoint.to_string();
+    let now = Instant::now();
+    {
+        let map = cache().lock().unwrap();
+        if let Some(entry) = map.get(&key) {
+            if entry.expires_at > now {
+                return entry.addr.clone();
+            }
+        }
+    }
+
+    // Resolve and cache the first address.
+    let port_u16: u16 = port.parse().unwrap_or(443);
+    let addr = match (host.as_str(), port_u16).to_socket_addrs() {
+        Ok(mut it) => match it.next() {
+            Some(a) => a.to_string(),
+            None => endpoint.to_string(),
+        },
+        Err(_) => endpoint.to_string(),
+    };
+
+    let mut map = cache().lock().unwrap();
+    map.insert(
+        key,
+        StickyEntry {
+            addr: addr.clone(),
+            expires_at: now + STICKY_TTL,
+        },
+    );
+    addr
+}
+
+/// Sticky version of `TcpStream::connect` for proxy node dials.
+pub fn sticky_connect(endpoint: &str) -> std::io::Result<TcpStream> {
+    TcpStream::connect(sticky_resolve(endpoint).as_str())
+}
+
+fn split_host_port(endpoint: &str) -> (String, String) {
+    // "[::1]:443" style.
+    if let Some(rest) = endpoint.strip_prefix('[') {
+        if let Some(idx) = rest.find(']') {
+            let host = &rest[..idx];
+            let port = rest[idx + 1..].strip_prefix(':').unwrap_or("").to_string();
+            return (host.to_string(), if port.is_empty() { "443".to_string() } else { port });
+        }
+    }
+    match endpoint.rsplit_once(':') {
+        Some((h, p)) => (h.to_string(), p.to_string()),
+        None => (endpoint.to_string(), "443".to_string()),
+    }
+}
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+
+    #[test]
+    fn ip_endpoint_passthrough() {
+        assert_eq!(sticky_resolve("1.2.3.4:443"), "1.2.3.4:443");
+        assert_eq!(sticky_resolve("1.2.3.4"), "1.2.3.4");
+        assert_eq!(sticky_resolve("[::1]:443"), "[::1]:443");
+    }
+
+    #[test]
+    fn domain_endpoint_is_cached_and_stable() {
+        let a = sticky_resolve("localhost:18080");
+        let b = sticky_resolve("localhost:18080");
+        assert_eq!(a, b, "same endpoint must keep the same resolved address");
+        assert!(
+            a.starts_with("127.0.0.1:18080") || a.starts_with("[::1]:18080"),
+            "localhost should resolve to loopback, got {a}"
+        );
+    }
+
+    #[test]
+    fn distinct_endpoints_are_distinct_cache_keys() {
+        let a = sticky_resolve("localhost:18081");
+        let b = sticky_resolve("localhost:18082");
+        assert!(a.ends_with(":18081"));
+        assert!(b.ends_with(":18082"));
+    }
+
+    #[test]
+    fn split_host_port_handles_ipv6() {
+        assert_eq!(split_host_port("[2001:db8::1]:8443"), ("2001:db8::1".to_string(), "8443".to_string()));
+        assert_eq!(split_host_port("example.com:443"), ("example.com".to_string(), "443".to_string()));
+    }
+}
diff --git a/crates/dae-outbound-stream/src/grpc.rs b/crates/dae-outbound-stream/src/grpc.rs
index 428b12d..e3d2d1d 100644
--- a/crates/dae-outbound-stream/src/grpc.rs
+++ b/crates/dae-outbound-stream/src/grpc.rs
@@ -370,7 +370,7 @@ pub fn grpc_hunk_exchange(
     payload: &[u8],
     timeout: Duration,
 ) -> Result<GrpcLifecycleReport, OutboundError> {
-    let mut stream = TcpStream::connect(endpoint)
+    let mut stream = dae_outbound_core::sticky::sticky_connect(endpoint)
         .map_err(|err| OutboundError::BadSharedTransport(err.to_string()))?;
     set_timeout(&stream, timeout)?;
     stream
diff --git a/crates/dae-outbound-stream/src/http_proxy/dataplane.rs b/crates/dae-outbound-stream/src/http_proxy/dataplane.rs
index bf52c02..37954e2 100644
--- a/crates/dae-outbound-stream/src/http_proxy/dataplane.rs
+++ b/crates/dae-outbound-stream/src/http_proxy/dataplane.rs
@@ -1,5 +1,4 @@
 use std::io::{Read, Write};
-use std::net::TcpStream;
 use std::time::Duration;
 
 use crate::http_head::read_http_head_with_leftover;
@@ -24,7 +23,7 @@ pub fn connect_exchange(
     timeout: Duration,
 ) -> Result<HttpConnectExchangeReport, OutboundError> {
     let mut stream =
-        TcpStream::connect(proxy).map_err(|err| OutboundError::BadHttpProxy(err.to_string()))?;
+        dae_outbound_core::sticky::sticky_connect(proxy).map_err(|err| OutboundError::BadHttpProxy(err.to_string()))?;
     stream
         .set_read_timeout(Some(timeout))
         .map_err(|err| OutboundError::BadHttpProxy(err.to_string()))?;
diff --git a/crates/dae-outbound-stream/src/meek.rs b/crates/dae-outbound-stream/src/meek.rs
index 3de1cab..fed811a 100644
--- a/crates/dae-outbound-stream/src/meek.rs
+++ b/crates/dae-outbound-stream/src/meek.rs
@@ -91,7 +91,7 @@ pub fn meek_polling_exchange(
 ) -> Result<MeekRoundTripReport, OutboundError> {
     let mut echoed_payloads = Vec::with_capacity(writes.len());
     for body in writes {
-        let mut stream = TcpStream::connect(endpoint)
+        let mut stream = dae_outbound_core::sticky::sticky_connect(endpoint)
             .map_err(|err| OutboundError::BadSharedTransport(err.to_string()))?;
         set_timeout(&stream, timeout)?;
         stream
diff --git a/crates/dae-outbound-stream/src/mux.rs b/crates/dae-outbound-stream/src/mux.rs
index 404030f..51cac08 100644
--- a/crates/dae-outbound-stream/src/mux.rs
+++ b/crates/dae-outbound-stream/src/mux.rs
@@ -277,7 +277,7 @@ pub fn mux_frame_exchange(
     payload: &[u8],
     timeout: Duration,
 ) -> Result<MuxLifecycleReport, OutboundError> {
-    let mut stream = TcpStream::connect(endpoint)
+    let mut stream = dae_outbound_core::sticky::sticky_connect(endpoint)
         .map_err(|err| OutboundError::BadSharedTransport(err.to_string()))?;
     set_timeout(&stream, timeout)?;
     stream
diff --git a/crates/dae-outbound-stream/src/shadowsocks/aead.rs b/crates/dae-outbound-stream/src/shadowsocks/aead.rs
index 7306bdd..c45b65b 100644
--- a/crates/dae-outbound-stream/src/shadowsocks/aead.rs
+++ b/crates/dae-outbound-stream/src/shadowsocks/aead.rs
@@ -117,7 +117,7 @@ pub fn tcp_exchange(
     timeout: Duration,
 ) -> Result<ShadowsocksAeadTcpExchangeReport, OutboundError> {
     let mut stream =
-        TcpStream::connect(server).map_err(|err| OutboundError::BadShadowsocks(err.to_string()))?;
+        dae_outbound_core::sticky::sticky_connect(server).map_err(|err| OutboundError::BadShadowsocks(err.to_string()))?;
     stream
         .set_read_timeout(Some(timeout))
         .map_err(|err| OutboundError::BadShadowsocks(err.to_string()))?;
diff --git a/crates/dae-outbound-stream/src/shadowsocks/ss2022_tcp_dataplane.rs b/crates/dae-outbound-stream/src/shadowsocks/ss2022_tcp_dataplane.rs
index 8e65362..35e0aa8 100644
--- a/crates/dae-outbound-stream/src/shadowsocks/ss2022_tcp_dataplane.rs
+++ b/crates/dae-outbound-stream/src/shadowsocks/ss2022_tcp_dataplane.rs
@@ -1,5 +1,4 @@
 use std::io::{Read, Write};
-use std::net::TcpStream;
 use std::time::{Duration, SystemTime, UNIX_EPOCH};
 
 use aes::cipher::{BlockEncrypt, KeyInit as BlockKeyInit};
diff --git a/crates/dae-outbound-stream/src/shadowsocks/ss2022_tcp_dataplane/exchanges.rs b/crates/dae-outbound-stream/src/shadowsocks/ss2022_tcp_dataplane/exchanges.rs
index 76dc5ad..a76e71e 100644
--- a/crates/dae-outbound-stream/src/shadowsocks/ss2022_tcp_dataplane/exchanges.rs
+++ b/crates/dae-outbound-stream/src/shadowsocks/ss2022_tcp_dataplane/exchanges.rs
@@ -9,7 +9,7 @@ pub fn tcp_exchange(
     timeout: Duration,
 ) -> Result<Ss2022TcpExchangeReport, OutboundError> {
     let mut stream =
-        TcpStream::connect(server).map_err(|err| OutboundError::BadShadowsocks(err.to_string()))?;
+        dae_outbound_core::sticky::sticky_connect(server).map_err(|err| OutboundError::BadShadowsocks(err.to_string()))?;
     stream
         .set_read_timeout(Some(timeout))
         .map_err(|err| OutboundError::BadShadowsocks(err.to_string()))?;
diff --git a/crates/dae-outbound-stream/src/shared_transport/reality.rs b/crates/dae-outbound-stream/src/shared_transport/reality.rs
index 56d161b..361d44d 100644
--- a/crates/dae-outbound-stream/src/shared_transport/reality.rs
+++ b/crates/dae-outbound-stream/src/shared_transport/reality.rs
@@ -136,7 +136,7 @@ pub fn reality_mutation_exchange(
     payload: &[u8],
     timeout: Duration,
 ) -> Result<RealityMutationReport, OutboundError> {
-    let mut stream = TcpStream::connect(endpoint)
+    let mut stream = dae_outbound_core::sticky::sticky_connect(endpoint)
         .map_err(|err| OutboundError::BadSharedTransport(err.to_string()))?;
     set_timeout(&stream, timeout)?;
     write_reality_harness_message(&mut stream, options, payload)?;
diff --git a/crates/dae-outbound-stream/src/socks5/dataplane.rs b/crates/dae-outbound-stream/src/socks5/dataplane.rs
index f8ae8b8..98449d5 100644
--- a/crates/dae-outbound-stream/src/socks5/dataplane.rs
+++ b/crates/dae-outbound-stream/src/socks5/dataplane.rs
@@ -1,5 +1,4 @@
 use std::io::{Read, Write};
-use std::net::TcpStream;
 use std::time::Duration;
 
 use dae_outbound_core::error::OutboundError;
@@ -37,7 +36,7 @@ pub fn tcp_connect_exchange(
     timeout: Duration,
 ) -> Result<Socks5TcpExchangeReport, OutboundError> {
     let mut stream =
-        TcpStream::connect(proxy).map_err(|err| OutboundError::BadSocks5Reply(err.to_string()))?;
+        dae_outbound_core::sticky::sticky_connect(proxy).map_err(|err| OutboundError::BadSocks5Reply(err.to_string()))?;
     stream
         .set_read_timeout(Some(timeout))
         .map_err(|err| OutboundError::BadSocks5Reply(err.to_string()))?;
diff --git a/crates/dae-outbound-stream/src/xhttp/http1.rs b/crates/dae-outbound-stream/src/xhttp/http1.rs
index fad2cd1..a264437 100644
--- a/crates/dae-outbound-stream/src/xhttp/http1.rs
+++ b/crates/dae-outbound-stream/src/xhttp/http1.rs
@@ -35,7 +35,7 @@ pub fn xhttp_packet_exchange(
     payload: &[u8],
     timeout: Duration,
 ) -> Result<XHttpLifecycleReport, OutboundError> {
-    let mut stream = TcpStream::connect(endpoint)
+    let mut stream = dae_outbound_core::sticky::sticky_connect(endpoint)
         .map_err(|err| OutboundError::BadSharedTransport(err.to_string()))?;
     set_timeout(&stream, timeout)?;
     let request = xhttp_packet_request(options, payload)?;
